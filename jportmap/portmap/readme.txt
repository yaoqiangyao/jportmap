A port mapping tool. 
It work as proxy server, mapping incoming socket connection to outcoming socket connection,.
Use it, you can connect to machines that are not connect directly.

To add or remove port mapping rule, just go where portmap.jar being and change ./conf/portmap.conf 
To start portmap, run start.sh
To stop, run stop.sh


Try it!